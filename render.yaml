services:
  - type: web
    name: company-compliance-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:appgit add .
git commit -m "Prepare for deployment"
git push